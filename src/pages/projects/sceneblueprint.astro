---
// SceneBlueprint 项目详情页
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SceneBlueprint 场景蓝图框架 - zgx197</title>
  <meta name="description" content="面向 Unity 的场景级蓝图编辑框架，提供 DSL 定义、可视化编辑、导出契约与运行时解释执行的一体化工具链。">
  <style>
    :root {
      --bg: #f8f9fa;
      --card-bg: #fff;
      --text: #1a1a1a;
      --text-secondary: #555;
      --text-muted: #888;
      --accent: #2563eb;
      --accent-hover: #1d4ed8;
      --border: #e8e8e8;
      --shadow: 0 1px 3px rgba(0,0,0,0.05);
      --shadow-hover: 0 4px 12px rgba(0,0,0,0.08);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif;
      line-height: 1.6;
      color: var(--text);
      background: var(--bg);
      min-height: 100vh;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      padding: 48px 24px;
    }

    .nav {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 32px;
    }

    .nav a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 14px;
      transition: color 0.2s;
    }

    .nav a:hover {
      color: var(--accent);
    }

    .nav-separator {
      color: var(--text-muted);
      font-size: 14px;
    }

    .project-header {
      margin-bottom: 40px;
    }

    .project-title {
      font-size: 32px;
      font-weight: 700;
      margin-bottom: 12px;
    }

    .project-subtitle {
      font-size: 16px;
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .project-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      font-size: 14px;
      color: var(--text-muted);
    }

    .project-meta span {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .media-section {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 40px;
      margin-bottom: 32px;
      text-align: center;
    }

    .media-placeholder {
      aspect-ratio: 16/9;
      background: var(--bg);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
      font-size: 14px;
      border: 2px dashed var(--border);
    }

    .media-caption {
      margin-top: 16px;
      font-size: 14px;
      color: var(--text-muted);
    }

    .section {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 32px;
      margin-bottom: 24px;
    }

    .section-title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 20px;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--border);
    }

    .section-content {
      font-size: 15px;
      line-height: 1.8;
      color: var(--text-secondary);
    }

    .section-content p {
      margin-bottom: 16px;
    }

    .section-content p:last-child {
      margin-bottom: 0;
    }

    .highlight-list {
      list-style: none;
      padding: 0;
    }

    .highlight-list li {
      position: relative;
      padding-left: 20px;
      margin-bottom: 16px;
      font-size: 15px;
      line-height: 1.7;
      color: var(--text-secondary);
    }

    .highlight-list li:last-child {
      margin-bottom: 0;
    }

    .highlight-list li::before {
      content: '▸';
      position: absolute;
      left: 0;
      color: var(--accent);
      font-weight: 600;
    }

    .highlight-title {
      font-weight: 600;
      color: var(--text);
      display: block;
      margin-bottom: 4px;
    }

    .tech-stack {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .tech-tag {
      padding: 8px 16px;
      background: var(--bg);
      border-radius: 20px;
      font-size: 14px;
      color: var(--text-secondary);
    }

    .link-list {
      list-style: none;
      padding: 0;
    }

    .link-list li {
      margin-bottom: 12px;
    }

    .link-list a {
      color: var(--accent);
      text-decoration: none;
      font-size: 15px;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .link-list a:hover {
      text-decoration: underline;
    }

    .link-list .meta {
      font-size: 13px;
      color: var(--text-muted);
      margin-left: 8px;
    }

    @media (max-width: 640px) {
      .container {
        padding: 24px 16px;
      }

      .project-title {
        font-size: 24px;
      }

      .media-section {
        padding: 24px;
      }

      .section {
        padding: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <nav class="nav">
      <a href="/">首页</a>
      <span class="nav-separator">/</span>
      <a href="/about">关于</a>
      <span class="nav-separator">/</span>
      <span style="color: var(--text); font-size: 14px;">SceneBlueprint 场景蓝图框架</span>
    </nav>

    <header class="project-header">
      <h1 class="project-title">SceneBlueprint 场景蓝图框架</h1>
      <p class="project-subtitle">
        面向 Unity 的场景级蓝图编辑框架。围绕 DSL 定义、可视化节点图编辑、导出契约与运行时解释执行构建完整工具链，目标是在“制作蓝图”和“执行蓝图”之间建立稳定边界，支撑 TA、策划工具与项目级玩法流程系统搭建。
      </p>
      <div class="project-meta">
        <span>📦 Unity Package</span>
        <span>👤 独立开发</span>
        <span>🧰 Editor + Runtime</span>
      </div>
    </header>

    <div class="media-section">
      <div class="media-placeholder">
        [ 蓝图编辑器截图 / DSL 示例 / 运行时调试画面占位区 ]
      </div>
      <p class="media-caption">
        可放置主工作台界面、.sbdef 定义示例、运行时状态调试或导出链路示意图
      </p>
    </div>

    <div class="section">
      <h2 class="section-title">核心定位</h2>
      <div class="section-content">
        <p>
          SceneBlueprint 不是单纯的节点编辑器，而是一套正式的场景蓝图框架。它把“编辑器制作流程”和“运行时执行流程”拆成两个清晰子系统，让蓝图资产能够在编辑器侧被定义、校验、编译与导出，再由运行时侧稳定加载、调度和解释执行。
        </p>
        <p>
          这个设计使项目既适合在单一 Unity 工程内使用，也支持编辑器工程与运行时工程拆分，方便团队把策划制作、TA 工具开发与正式游戏运行时接入分开组织。
        </p>
      </div>
    </div>

    <div class="section">
      <h2 class="section-title">架构设计</h2>
      <ul class="highlight-list">
        <li>
          <span class="highlight-title">编辑器与运行时双子系统</span>
          将蓝图制作与蓝图执行正式拆分，编辑器侧聚焦 authoring、校验、导出与预览，运行时侧聚焦加载、调度、解释执行与状态管理，避免单体式工具把制作逻辑和运行时逻辑耦合在一起。
        </li>
        <li>
          <span class="highlight-title">稳定的数据边界</span>
          通过 `BlueprintAsset`、`SceneBlueprintData` 与 `BlueprintFrame` 三类核心载体，把编辑器工作资产、导出契约和运行时真相明确区分，形成可维护、可扩展的蓝图生命周期。
        </li>
        <li>
          <span class="highlight-title">解释器式运行时</span>
          运行时以 `BlueprintLoader`、`BlueprintRunner`、System 调度和状态域为核心，按声明、语义、计划、状态、执行的分层思路组织蓝图执行过程，更适合复杂玩法流程与调试需求。
        </li>
      </ul>
    </div>

    <div class="section">
      <h2 class="section-title">工具链亮点</h2>
      <ul class="highlight-list">
        <li>
          <span class="highlight-title">`.sbdef` DSL + 代码生成</span>
          以 `.sbdef` 作为单一事实来源，声明 Action、Marker、Signal、Tag Dimension 等能力，再由 Unity Importer 与外部 `SbdefGen` CLI 协同生成运行时与编辑器注册代码，降低手写样板与定义漂移。
        </li>
        <li>
          <span class="highlight-title">节点图编辑与专项工作台</span>
          基于 `com.zgx197.nodegraph` 构建蓝图编辑窗口，并支持工作区恢复、节点专用工作台、分析面板、编译预览与测试窗口，覆盖从 authoring 到验证的核心制作链路。
        </li>
        <li>
          <span class="highlight-title">空间与场景绑定体系</span>
          提供 Marker、Annotation、Spatial 抽象、2D/3D 适配器与场景对象绑定恢复机制，让蓝图不只描述流程，还能正式接入场景空间语义与编辑器交互。
        </li>
      </ul>
    </div>

    <div class="section">
      <h2 class="section-title">运行时与调试能力</h2>
      <ul class="highlight-list">
        <li>
          <span class="highlight-title">黑板、信号与状态域</span>
          运行时维护黑板、事件、端口状态、时间与生命周期信息，为蓝图系统提供稳定的执行上下文，并支持更复杂的业务 System 逐层接入。
        </li>
        <li>
          <span class="highlight-title">快照、历史与调试视图</span>
          提供运行时状态快照、帧历史记录与调试控制能力，便于排查执行路径、观察状态演变，也为后续测试、回放和诊断工具留出了空间。
        </li>
        <li>
          <span class="highlight-title">知识服务与 AI 辅助</span>
          在编辑器侧内建知识服务、知识清单与 AI Chat 面板，用于将文档、上下文与工具操作连接起来，进一步提升复杂蓝图系统的理解与维护效率。
        </li>
      </ul>
    </div>

    <div class="section">
      <h2 class="section-title">技术栈</h2>
      <div class="tech-stack">
        <span class="tech-tag">Unity 2021.3+</span>
        <span class="tech-tag">C#</span>
        <span class="tech-tag">Editor Tooling</span>
        <span class="tech-tag">DSL</span>
        <span class="tech-tag">Code Generation</span>
        <span class="tech-tag">Node Graph</span>
        <span class="tech-tag">Runtime Interpreter</span>
        <span class="tech-tag">SignalBus</span>
        <span class="tech-tag">State Snapshot</span>
        <span class="tech-tag">AI Assistant</span>
      </div>
    </div>

    <div class="section">
      <h2 class="section-title">相关链接</h2>
      <ul class="link-list">
        <li>
          <a href="/about">← 返回项目列表</a>
        </li>
        <li>
          <a href="https://github.com/zgx197/com.zgx197.sceneblueprint" target="_blank" rel="noopener">
            GitHub 仓库
            <span class="meta">查看源码与 README</span>
          </a>
        </li>
      </ul>
    </div>
  </div>
</body>
</html>
